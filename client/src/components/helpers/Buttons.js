






export const LightBulb1Off = ({ handleChange }) => {
  return <button id='bulb1' className='bulb' name='brightness' value={1} onClick={handleChange}></button>
}

export const LightBulb1On = ({ handleChange }) => {
  return <button id='bulb1' className='bulb-on' name='brightness' value={1} onClick={handleChange}></button>
}

export const LightBulb2Off = ({ handleChange }) => {
  return <button id='bulb2' className='bulb' name='brightness' value={2} onClick={handleChange}></button>
}

export const LightBulb2On = ({ handleChange }) => {
  return <button id='bulb2' className='bulb-on' name='brightness' value={2} onClick={handleChange}></button>
}

export const LightBulb3Off = ({ handleChange }) => {
  return <button id='bulb3' className='bulb' name='brightness' value={3} onClick={handleChange}></button>
}

export const LightBulb3On = ({ handleChange }) => {
  return <button id='bulb3' className='bulb-on' name='brightness' value={3} onClick={handleChange}></button>
}

export const LightBulb4Off = ({ handleChange }) => {
  return <button id='bulb4' className='bulb' name='brightness' value={4} onClick={handleChange}></button>
}

export const LightBulb4On = ({ handleChange }) => {
  return <button id='bulb4' className='bulb-on' name='brightness' value={4} onClick={handleChange}></button>
}

export const LightBulb5Off = ({ handleChange }) => {
  return <button id='bulb5' className='bulb' name='brightness' value={5} onClick={handleChange}></button>
}

export const LightBulb5On = ({ handleChange }) => {
  return <button id='bulb5' className='bulb-on' name='brightness' value={5} onClick={handleChange}></button>
}